<!DOCTYPE html>
<html lang="en">
 <%- include('../partials/head.ejs') %>

 <body>
    <%- include('../partials/header.ejs')%>
    <h2> Combinations</h2>

    <div>
        <% for(let i = 0; i < lessons.length; i++) { %>
            <a href="/lessons/<%=lessons[i]._id%>"><%= lessons[i].combo, lessons[i].combo, lessons[i].discription %></a> 
            <br />
             <!-- <%= //lessons[i].track %><br />
             <%= //lessons[i].combo %><br />
             <%= //lessons[i].discription %><br /> -->
        <% } %>
    </div> 
    <button onClick='location=href="./new"' type=""button>New Combination</button>
    <button onClick="location=href='../'" type="button">Back</button>
</body>
</html>